package de.hsh.zahlenarraytest.Zeitmesser;

public class Zeitmesser {
    private String zeitmesser;
    private long time1 = 0, time2 = 0;
    
    public void start() {
        if (zeitmesser == "läuft") {
            throw new IllegalArgumentException("Darf nicht beim Zeitmessen aufgerufen werden");
        }
        zeitmesser = "läuft";
        time1 = System.currentTimeMillis();
    }
    
    public void stop() {
        if (zeitmesser == "stoppt") {
            throw new IllegalArgumentException("Es wurde schon gestoppt");
        }
        zeitmesser = "stop";
        time1 = System.currentTimeMillis();
    }
    
    public long getGemesseneGesamtzeit() {
        if (zeitmesser == "läuft") {
            throw new IllegalArgumentException("Darf nicht beim Zeitmessen aufgerufen werden");
        }
        return time2 - time1;
    }
}
